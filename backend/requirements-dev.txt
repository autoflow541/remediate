# Dev/test-only dependencies (NOT installed into the production image).
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest            # from the backend/ directory
-r requirements.txt
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient (test_jobs.py)
