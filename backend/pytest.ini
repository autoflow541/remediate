[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q -p no:cacheprovider
# pikepdf-dependent tests self-skip via pytest.importorskip when it's absent,
# so the pure-logic tier still runs on a bare interpreter.
